import React from 'react';
import type { Props } from '@theme/DocPaginator';
import { DocPaginator } from '@site/src/components/paginator';

export default (props: Props) => <DocPaginator {...props} />;
