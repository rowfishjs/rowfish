import React from 'react';
import type { Props } from '@theme/BlogListPaginator';
import { BlogListPaginator } from '@site/src/components/paginator/blog.list';

export default (props: Props) => <BlogListPaginator {...props} />;
