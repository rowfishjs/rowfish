import React from 'react';
import type { Props } from '@theme/BlogPostPaginator';
import { BlogPostPaginator } from '@site/src/components/paginator/blog.post';

export default (props: Props) => <BlogPostPaginator {...props} />;
