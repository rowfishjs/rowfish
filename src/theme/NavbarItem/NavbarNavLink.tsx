import React from 'react';
import type { Props } from '@theme/NavbarItem/NavbarNavLink';
import { NavbarNavLink } from '@site/src/components/layout/navbar/link';

export default (props: Props) => <NavbarNavLink {...props} />;
